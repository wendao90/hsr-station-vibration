# Core scientific computing
numpy>=1.24
scipy>=1.11
pandas>=2.0
matplotlib>=3.7

# Data I/O
xlrd>=2.0          # read legacy .xls exported by phyphox
openpyxl>=3.1      # read .xlsx if needed
soundfile>=0.12    # read/write wav
imageio-ffmpeg>=0.4  # bundled ffmpeg for m4a → wav conversion

# Manuscript generation
python-docx>=1.1

# Screenshot capture (Figure 3)
playwright>=1.40
